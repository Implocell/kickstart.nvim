return {
  {
    'seblyng/roslyn.nvim',
    ft = 'cs',
    opts = {
      -- specific options for the language server if needed
    },
    init = function()
      -- specific initialization logic if needed
    end,
  },
}
